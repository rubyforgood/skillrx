<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div class="<%= tailwind_flash_class(name) %> p-4 mb-4 rounded-md flex items-center justify-between" role="alert">
      <span><%= msg %></span>
      <button type="button" class="ml-4 text-lg font-semibold hover:opacity-70" onclick="this.parentElement.remove()">×</button>
    </div>
  <% end %>
<% end %>
